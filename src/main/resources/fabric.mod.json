{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "version": "${version}",
  "authors": ["${authors}"],
  "contributors": ["${contributors}"],
  "contact": {
    "sources": "${sources_url}",
    "issues": "${issues_url}",
    "homepage": "${homepage_url}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_url}"
      }
    }
  },
  "description": "${description}",
  "license": "${license}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "me.pajic.tiered_chests.platform.fabric.FabricEntrypoint"
    ],
    "client": [
      "me.pajic.tiered_chests.platform.fabric.FabricClientEntrypoint"
    ]
  }
}
